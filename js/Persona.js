class Persona {
    constructor(rol, nombre, edad, fechaIngreso) {
        this.rol = rol;
        this.nombre = nombre;
        this.edad = parseInt(edad);
        this.fechaIngreso = fechaIngreso;
    }
}